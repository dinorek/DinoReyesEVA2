<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:minHeight="?attr/actionBarSize"
    android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
    app:popupTheme="@style/Theme.AppCompat.Light"
    android:id="@+id/toolbar"
    android:background="@color/colorPrimaryDark"
    android:layout_height="wrap_content">
    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="horizontal"
        android:layout_height="wrap_content">
        <ImageView
            android:layout_width="@dimen/imagen_logo"
            android:src="@drawable/ic_brote"
            android:layout_marginLeft="@dimen/centrar_img"
            android:layout_height="@dimen/imagen_logo">
        </ImageView>
        <TextView
            android:layout_width="match_parent"
            android:text="EXAMEN PREVENCIÓN COVID"
            android:layout_marginLeft="@dimen/titulo_app_size"
            android:textSize="@dimen/titulo_app_size"
            android:layout_gravity="center"
            android:textStyle="italic"
            android:layout_height="wrap_content">
        </TextView>
    </LinearLayout>


</androidx.appcompat.widget.Toolbar>